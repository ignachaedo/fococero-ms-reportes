apiVersion: v1
kind: Service
metadata:
  name: ms-reportes
  namespace: fococero
spec:
  selector:
    app: ms-reportes
  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004
  type: ClusterIP
